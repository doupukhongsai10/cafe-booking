{
  "name": "cafe-booking",
  "version": "1.0.0",
  "private": true,
  "description": "Café Booking & Management Platform",
  "scripts": {
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio",
    "prisma:generate": "prisma generate"
  },
  "devDependencies": {
    "prisma": "^5.10.0"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0"
  }
}
